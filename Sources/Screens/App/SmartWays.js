import { StyleSheet, View } from 'react-native';
import { RNContainer, RNHeader } from '../../Common';

const SmartWays = () => {
  const onSettigPress = () => {};

  return (
    <RNContainer>
      <RNHeader
        title={'Smart Way to Earn Money'}
        onSettigPress={onSettigPress}></RNHeader>
    </RNContainer>
  );
};

export default SmartWays;

const styles = StyleSheet.create({});
